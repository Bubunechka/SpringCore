<html>
    <body>
    <div>
        <form method="post">
            <input type="text"name="text" placeholder="Введите сообщение"/>
            <input type="type" name="tag" placeholder="Тэг">
            <button> type="submit"> Добавить</button>>

        </form>
    </div>
        <head>
            <div>Список сообщений</div>
            {{#messages}}
                <div>
                    <b>{{id}}</b>
                    <span>{{text}}</span>
                    <i>{{tag}}</i>
                </div>
            {{/messages}}
        </head>
    </body>
</html>